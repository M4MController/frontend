<div class="container">
  <div class="row">
    {{page-caption text="Мои объекты" icon="icon-diagram" class="col-md-8"}}
  </div>
  <div class="row havings">
    {{#each model as |object|}}
      {{object-card object=object}}
    {{/each}}
  </div>
</div>
