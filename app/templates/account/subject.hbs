<div class="grid-static full-width">
  {{#each (await model.sensors) as |sensor|}}
    {{sensor-card sensor=sensor}}
  {{/each}}
</div>

{{#if showAddModal}}
  {{modals/add-controller
    onClose=(action 'set' 'showAddModal' false)
    onAdd=(action 'addControllerAction')
  }}
{{/if}}
